spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/syne_blog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: postgres
    password: 201314

  data:
    redis:
      password: 201314

jwt:
  secret: dev-local-secret-key-for-development-only

# MyBatis-Plus SQL 日志（开发环境开启）
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 日志级别（开发环境详细日志）
logging:
  level:
    com.syne.server: debug
    com.baomidou.mybatisplus: debug
    org.springframework.security: debug
