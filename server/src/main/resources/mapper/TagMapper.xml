<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syne.server.mapper.TagMapper">

    <!-- 标签列表结果映射 -->
    <resultMap id="TagListVOMap" type="com.syne.server.model.vo.TagListVO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="slug" column="slug"/>
        <result property="color" column="color"/>
        <result property="articleCount" column="article_count"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 查询标签列表 -->
    <select id="selectTagList" resultMap="TagListVOMap">
        SELECT
            t.id,
            t.name,
            t.slug,
            COALESCE(t.color, '#409EFF') AS color,
            COALESCE(atc.article_count, 0) AS article_count,
            t.create_time,
            t.update_time
        FROM tags t
        LEFT JOIN (
            SELECT
                at.tag_id,
                COUNT(*) AS article_count
            FROM article_tags at
            INNER JOIN articles a ON a.id = at.article_id
            WHERE at.deleted = 0
              AND a.deleted = 0
            GROUP BY at.tag_id
        ) atc ON atc.tag_id = t.id
        WHERE t.deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (t.name ILIKE CONCAT('%', #{keyword}, '%') OR t.slug ILIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <choose>
            <when test="sortBy == 'usage_count' or sortBy == 'article_count'">
                ORDER BY COALESCE(atc.article_count, 0)
            </when>
            <when test="sortBy == 'create_time'">
                ORDER BY t.create_time
            </when>
            <when test="sortBy == 'name'">
                ORDER BY t.name
            </when>
            <otherwise>
                ORDER BY COALESCE(atc.article_count, 0)
            </otherwise>
        </choose>
        <choose>
            <when test="sortOrder == 'asc'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- 查询标签数量 -->
    <select id="countTags" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM tags
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (name ILIKE CONCAT('%', #{keyword}, '%') OR slug ILIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <!-- 查询所有标签列表 -->
    <select id="selectAllTagList" resultMap="TagListVOMap">
        SELECT
            t.id,
            t.name,
            t.slug,
            COALESCE(t.color, '#409EFF') AS color,
            COALESCE(atc.article_count, 0) AS article_count,
            t.create_time,
            t.update_time
        FROM tags t
        LEFT JOIN (
            SELECT
                at.tag_id,
                COUNT(*) AS article_count
            FROM article_tags at
            INNER JOIN articles a ON a.id = at.article_id
            WHERE at.deleted = 0
              AND a.deleted = 0
            GROUP BY at.tag_id
        ) atc ON atc.tag_id = t.id
        WHERE t.deleted = 0
        ORDER BY COALESCE(atc.article_count, 0) DESC, t.create_time DESC
    </select>

    <!-- 检查标签是否被文章使用 -->
    <select id="countArticleTagsByTagId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM article_tags at
        INNER JOIN articles a ON a.id = at.article_id
        WHERE at.tag_id = #{tagId}
          AND at.deleted = 0
          AND a.deleted = 0
    </select>

</mapper>
