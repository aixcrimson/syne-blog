<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syne.server.mapper.ArticleTagMapper">

    <!-- 批量插入文章标签关联 -->
    <insert id="insertByArticleAndTags">
        INSERT INTO article_tags (article_id, tag_id, create_time, update_time, deleted)
        VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{articleId}, #{tagId}, NOW(), NOW(), 0)
        </foreach>
    </insert>

    <!-- 根据文章ID删除所有标签关联 -->
    <delete id="deleteByArticleId">
        UPDATE article_tags
        SET deleted = 1, update_time = NOW()
        WHERE article_id = #{articleId} AND deleted = 0
    </delete>

    <!-- 根据文章ID和标签ID列表删除特定关联 -->
    <delete id="deleteByArticleIdAndTags">
        UPDATE article_tags
        SET deleted = 1, update_time = NOW()
        WHERE article_id = #{articleId}
        AND tag_id IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
        AND deleted = 0
    </delete>

    <!-- 根据文章ID查询关联的标签ID列表 -->
    <select id="selectTagIdsByArticleId" resultType="java.lang.Long">
        SELECT tag_id
        FROM article_tags
        WHERE article_id = #{articleId}
          AND deleted = 0
        ORDER BY tag_id
    </select>
</mapper>